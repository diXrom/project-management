const API_URL = 'https://localhost:4444';

const API_PATH = {
  LOGIN: '/login',
  REGISTER: '/register',
};

const TAGS = {
  DEFAULT: { type: 'Posts', id: 'LIST' },
};

export { API_URL, API_PATH, TAGS };
